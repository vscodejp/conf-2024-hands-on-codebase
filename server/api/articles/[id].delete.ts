import { PrismaClient } from "@prisma/client";

const prisma = new PrismaClient();

/**
 * 記事を削除するAPI
 * @param event API Gatewayのイベントオブジェクト
 */
export default defineEventHandler(async (event) => {
  // TODO: copilotを使って処理を提案してもらいましょう
});
